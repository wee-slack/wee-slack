language: python
python:
  - "2.7"
  - "3.6"
matrix:
  allow_failures:
    - python: "3.6"

install:
  - 'pip install websocket-client pytest'

script: python -m pytest

notifications:
  irc: "chat.freenode.net#wee-slack-dev"
  email: false
